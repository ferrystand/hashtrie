# hashtrie
A high-performance CLI tool that deterministically partitions massive sorted hash:plain datasets into prefix-based shards, materialising a disk-backed trie for fast keyspace lookup and scalable storage
